export enum SyncUpdateableValues {
  reservations = 6,
  roomStatus = 6,
  rooms = 'date',
  rates = 'date',
  channel = 'date',
  mailing = 'date',
  calls = 8,
}

export enum SyncField {
  reservations = 'reservations',
  roomStatus = 'roomStatus',
  rooms = 'rooms',
  rates = 'rates',
  channel = 'channel',
  mailing = 'mailing',
  calls = 'calls',
}
